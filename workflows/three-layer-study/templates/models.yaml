schema_version: '1.0'
study_id: '{{study_id}}'
models: []
# Model profiles a generative or semantic-evaluator executor may bind to.
# Never put a key, token or endpoint secret here.
#
# - id: study-model
#   provider: openai
#   model: gpt-4.1-mini
#   capabilities: [structured-output]
#   parameters: {temperature: 0.7}
#   endpoint_ref: default        # resolved from local configuration, not inlined
